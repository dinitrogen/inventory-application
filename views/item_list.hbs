<h1>{{title}}</h1>

<ul>
    {{#each item_list}}
    <li>
        <a href={{this.url}}>{{this.name}}</a> ({{this.category.name}})
    </li>
    {{else}}
    <li>There are no items</li>
    
    {{/each}}
</ul>